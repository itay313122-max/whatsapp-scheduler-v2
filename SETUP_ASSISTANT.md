# העוזר האישי החכם — התקנה והפעלה

עוזר קולי פרטי מבוסס AI (מנוע חינמי **Gemini** כברירת מחדל, או **Claude** בתשלום). עובד בדפדפן, ניתן להתקנה כאפליקציה על **אייפון** ו**מקבוק**,
מסונכרן בין המכשירים, נעול בסיסמה — רק שלך.

הכתובת של העוזר בכל שרת: **`/assistant.html`**

---

## 🖥️ הכי מהיר — הרצה על המק שלך (בלחיצה כפולה)

רוצה שזה ירוץ רק על המחשב שלך, בלי ענן? עשה זאת:

1. **התקן פעם אחת:** [Node.js](https://nodejs.org) (גרסה LTS) ו-[Git](https://git-scm.com) אם עוד אין.
2. **הורד את הפרויקט** — פתח את אפליקציית **Terminal** והדבק:
   ```bash
   git clone -b claude/smart-voice-assistant-g1li66 https://github.com/itay313122-max/whatsapp-scheduler-v2.git
   cd whatsapp-scheduler-v2
   ```
3. **הפעל בלחיצה כפולה** — פתח את התיקייה ב-Finder ולחץ פעמיים על **`run-mac.command`**.
   - בפעם הראשונה מק עלול לחסום → **לחיצה ימנית → Open**.
   - הקובץ יתקין הכל לבד, יפתח לך את `.env` להדביק את המפתח **החינמי** `GEMINI_API_KEY`, ואז יפתח את העוזר בדפדפן ב-<http://localhost:3000/assistant.html>.
4. להריץ שוב בעתיד — פשוט לחיצה כפולה שוב על `run-mac.command`.

> חלופה למי שנוח לו ב-Terminal: `cp .env.example .env` (ערוך והדבק מפתח) → `npm install` → `npm run start:local`.

> להתקין כאייקון על המק: פתח את הכתובת ב-Chrome → אייקון **התקן** בשורת הכתובת.
> הערה: בהרצה מקומית הכתובת `localhost` עובדת רק על המק עצמו. כדי להגיע מהאייפון צריך פריסה (שלב 1 למטה) — או להשאיר את המק דלוק ולהשתמש ברשת המקומית.

---

## שלב 1 — פריסת השרת (פעם אחת, לגישה מכל מקום)

השרת (`server.js` בשורש הריפו) צריך לרוץ אונליין כדי שתגיע אליו מכל מקום. הכי פשוט — **Render** (יש תוכנית חינם, ו-`Dockerfile` כבר מוכן):

1. היכנס ל-<https://render.com> והתחבר עם GitHub.
2. **New → Web Service** → בחר את הריפו `whatsapp-scheduler-v2`.
3. הגדרות:
   - **Branch**: `claude/smart-voice-assistant-g1li66` (או `main` אחרי מיזוג)
   - **Runtime**: Docker (מזוהה אוטומטית מה-`Dockerfile`)
   - **Instance Type**: Free
4. **Environment → Add Environment Variable** — הגדר:
   | Key | Value | לְמה |
   |---|---|---|
   | `GEMINI_API_KEY` | המפתח החינמי שלך | **המוח החינמי.** משיגים בחינם ב-<https://aistudio.google.com/apikey> |
   | `APP_PASSWORD` | קוד סודי משלך | הנעילה הפרטית — רק מי שיודע אותו נכנס |
   | `PORT` | `3000` | (בד"כ אוטומטי ב-Render) |

   > רוצה איכות מקסימלית ומוכן לשלם? הגדר במקום זאת `LLM_PROVIDER=claude` ו-`ANTHROPIC_API_KEY=sk-ant-...` (מ-<https://console.anthropic.com>).
5. **Create Web Service**. אחרי ~דקה תקבל כתובת כמו `https://my-assistant.onrender.com`.

> יש קובץ blueprint מוכן ב-`render.assistant.yaml` — אפשר להשתמש בו אם מעדיפים "Blueprint" במקום הגדרה ידנית.

> ⚠️ בתוכנית החינם של Render השרת "נרדם" אחרי חוסר פעילות ולוקח כמה שניות להתעורר, ודיסק הנתונים אפמרי (אנשי הקשר עלולים להתאפס בדיפלוי מחדש). לשימוש רציני — תוכנית בתשלום עם Persistent Disk, או כל ספק אחר (Railway/Fly.io/VPS משלך).

---

## שלב 2 — התקנה על האייפון 📱

1. פתח ב-**Safari** את `https://<הכתובת-שלך>/assistant.html`.
2. הזן את קוד הגישה (`APP_PASSWORD`) כשמבקשים — הוא נשמר במכשיר.
3. כפתור **שיתוף** (ריבוע עם חץ) → **"הוסף למסך הבית"**.
4. עכשיו יש לך אייקון של אפליקציה 🎙️. פותחים אותו — זה מרגיש כמו אפליקציה אמיתית, במסך מלא.

> **חשוב לגבי קול באייפון:** Safari לא תומך בזיהוי דיבור בתוך הדף. לכן באייפון:
> - הקש על שדה הטקסט והשתמש בכפתור **המיקרופון של המקלדת** (הכתבה), **או**
> - הכן **קיצור סירי** (ראה שלב 4) — "היי סירי, עוזר".

---

## שלב 3 — התקנה על המקבוק 💻 (מסונכרן)

1. פתח את אותה כתובת ב-**Chrome** (מומלץ — תומך בדיבור מלא) והזן את **אותו** קוד גישה.
2. בשורת הכתובת מופיע אייקון **התקן** (⊕) → *Install*. עכשיו יש אפליקציה גם על המק.
3. **הסנכרון:** מכיוון ששני המכשירים פונים לאותו שרת עם אותו קוד — **אנשי הקשר משותפים** אוטומטית. מוסיפים "אמא" במק → זמין מיד באייפון, ולהפך.

> במק ב-Chrome זיהוי הדיבור עובד מלא, כולל "שיחה רציפה" (ידיים חופשיות) ושליחת וואטסאפ עם כרטיס אישור.

---

## שלב 4 — קיצור קולי "היי סירי" (אופציונלי, לאייפון)

מאפשר להפעיל את העוזר בקול בלי לפתוח את האפליקציה:

1. אפליקציית **קיצורים (Shortcuts)** → **+** → קיצור חדש.
2. פעולה **"מלל מוכתב" (Dictate Text)**.
3. פעולה **"קבל תוכן של כתובת URL" (Get Contents of URL)**:
   - URL: `https://<הכתובת-שלך>/api/ask`
   - Method: **POST**
   - Headers: `x-app-key` = קוד הגישה שלך, ו-`Content-Type` = `application/json`
   - Request Body: **JSON** → מפתח `text` שערכו = ה"מלל המוכתב".
4. פעולה **"קבל ערך מילון" (Get Dictionary Value)** → מפתח `reply`.
5. פעולה **"דבר טקסט" (Speak Text)** על התוצאה.
6. תן שם לקיצור, למשל **"עוזר"** → עכשיו: **"היי סירי, עוזר"** 🎙️.

> דרך סירי הוא עונה ומחפש באינטרנט מלא. פעולות ששולחות החוצה (וואטסאפ) יבקשו לפתוח את האפליקציה ולאשר — כדי ששום דבר לא נשלח בלי אישורך.

---

## מה מחובר עכשיו — ומה השלב הבא

| יכולת | סטטוס |
|---|---|
| 🔍 חיפוש והבנה באינטרנט | ✅ עובד |
| 💬 שליחה/תזמון WhatsApp (עם אישור) | ✅ עובד |
| 👥 אנשי קשר מסונכרנים | ✅ עובד |
| 🔒 נעילה פרטית (סיסמה) | ✅ עובד |
| 📅 יומן (Google Calendar) | 🔧 מסגרת מוכנה — צריך חיבור OAuth |
| 📧 מייל (Gmail) | 🔧 מסגרת מוכנה — צריך חיבור OAuth |
| 🎵 ספוטיפיי | 🔧 מסגרת מוכנה — צריך חיבור OAuth |

**החיבור לספוטיפיי / מייל / יומן** דורש רישום אפליקציה אצל כל ספק (Client ID/Secret), הגדרת כתובת חזרה (redirect) בשרת שלך, ואישור חד-פעמי מולך. זה השלב הבא — ראה למטה.

---

## בדיקות

```bash
npm test        # בדיקות יחידה של לוגיקת העוזר
npm start       # הרצה מקומית → http://localhost:3000/assistant.html
```
