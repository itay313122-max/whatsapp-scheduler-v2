// ─────────────────────────────────────────────────────────────
// STAGE 1 fills this in.
//
// This module will initialize the Firebase JS SDK (Auth, Firestore,
// Storage) using config pulled from a .env file, and export the
// initialized services (auth, db, storage) for the rest of the app.
//
// Auth will use AsyncStorage persistence via
// initializeAuth(app, { persistence: getReactNativePersistence(AsyncStorage) })
// so the user stays logged in between app launches.
// ─────────────────────────────────────────────────────────────

export const PLACEHOLDER = true;
